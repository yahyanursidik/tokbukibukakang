---
import PlaceholderPage from '@/components/PlaceholderPage.astro';
import BaseLayout from '@/layouts/BaseLayout.astro';
import { placeholderPages } from '@/config/site';
import { getPublicPageBySlug } from '@/lib/content/public';

export const prerender = false;

const page = placeholderPages.contact;
const pageMeta = await getPublicPageBySlug('kontak');
---

<BaseLayout title={pageMeta?.seoTitle ?? 'Kontak'} description={pageMeta?.seoDescription ?? page.description}>
  <PlaceholderPage {...page} />
</BaseLayout>
